# SECRET_KEY 설정 가이드

## 📋 개요

전화번호 암호화를 위해 `.env` 파일에 `SECRET_KEY`를 설정해야 합니다.

이 키는 **보안카드 역할**을 하며, 암호화/복호화에 사용됩니다.

## 🔐 SECRET_KEY 생성 방법

### 방법 1: Python으로 자동 생성 (권장)

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

이 명령어를 실행하면 다음과 같은 키가 생성됩니다:
```
gAAAAABk... (64자리 문자열)
```

### 방법 2: 온라인 생성기 사용

- https://generate-secret.vercel.app/64 에서 64자리 랜덤 문자열 생성

### 방법 3: 직접 입력

임의의 64자리 이상의 문자열을 사용할 수 있습니다.

## ⚙️ .env 파일 설정

프로젝트 루트 디렉토리의 `.env` 파일에 다음을 추가하세요:

```env
# 전화번호 암호화 키 (보안카드 역할)
SECRET_KEY=여기에_생성한_키_입력
```

### 예시:

```env
SECRET_KEY=gAAAAABkZxYvF7Kj8LmN9OpQrStUvWxYzA1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4
```

## ⚠️ 중요 주의사항

1. **절대 공개하지 마세요**: 이 키가 노출되면 암호화된 모든 데이터를 복호화할 수 있습니다.
2. **백업 필수**: 키를 잃어버리면 암호화된 데이터를 복호화할 수 없습니다.
3. **환경별 분리**: 개발/운영 환경에 따라 다른 키를 사용하는 것을 권장합니다.
4. **Git에 커밋 금지**: `.env` 파일은 반드시 `.gitignore`에 포함되어 있어야 합니다.

## 🧪 테스트

설정이 올바른지 테스트하려면:

```bash
# 암호화 테스트
python api/utils/cipher_service.py encrypt "010-6664-3744"

# 복호화 테스트 (위에서 나온 암호화된 문자열 사용)
python api/utils/cipher_service.py decrypt "gAAAAABk..."
```

정상적으로 작동하면 암호화/복호화된 문자열이 출력됩니다.

## 🔄 키 변경 시 주의사항

**중요**: 기존 키로 암호화된 데이터가 있는 경우, 새 키로 변경하면 기존 데이터를 복호화할 수 없습니다.

키 변경이 필요한 경우:

1. 기존 데이터를 모두 복호화하여 평문으로 변환
2. 새 키로 재암호화
3. `.env` 파일의 `SECRET_KEY` 업데이트

## 📚 관련 파일

- 암호화 서비스: `api/utils/cipher_service.py`
- Node.js 유틸리티: `api/utils/cipher.js`
- 데이터베이스 스키마: `database/schemas/features/analytics/sales-data-schema.sql`

