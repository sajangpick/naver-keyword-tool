# 🎬 제미나이 Veo 3.1 영상 생성 기능 구현

## 📋 구현 내용

제미나이 Veo 3.1 API를 사용하여 영상 생성 기능을 추가했습니다.

### 현재 영상 생성 우선순위

1. **1순위: Runway API** (기존)
   - Gen-4 Image to Video 모델 사용
   - 3-10초 영상 생성 가능
   - SDK 또는 HTTP API 사용

2. **2순위: Gemini Veo 3.1** (신규 추가)
   - Google의 최신 영상 생성 모델
   - 5-8초 영상 생성 가능
   - 720p 또는 1080p 해상도 지원
   - 오디오 포함 (대사, 효과음, 음악)

3. **3순위: Runway HTTP 폴백** (기존)
   - Runway SDK 실패 시 HTTP API로 재시도

### 작동 방식

```
사용자가 영상 생성 요청
  ↓
1. Runway API 시도
  ↓ (실패 시)
2. Runway HTTP API 시도
  ↓ (실패 시)
3. Gemini Veo 3.1 API 시도
  ↓ (모두 실패 시)
에러 반환
```

## ⚙️ 설정 방법

### 1. 환경변수 확인

**Render 대시보드** 또는 **로컬 .env 파일**에 다음 환경변수가 설정되어 있어야 합니다:

```env
GEMINI_API_KEY=your_gemini_api_key_here
RUNWAY_API_KEY=your_runway_api_key_here  # 선택사항 (Veo만 사용 시 불필요)
```

### 2. Gemini Veo 3.1 접근 권한

⚠️ **중요**: Gemini Veo 3.1을 사용하려면:

- ✅ **유료 결제가 활성화된 계정** (Paid Tier) 필요
- ✅ **Tier 1 이상 접근 권한** 필요
- ✅ Google AI Studio에서 Veo 3.1 모델 접근 권한 확인

**확인 방법:**
1. https://aistudio.google.com 접속
2. API 키 확인
3. Veo 3.1 모델 사용 가능 여부 확인

### 3. 가격 정보

| 모델 | 가격 | 특징 |
|------|------|------|
| Veo 3 | 초당 $0.40 | 고품질, 오디오 포함 |
| Veo 3 Fast | 초당 $0.15 | 빠른 생성, 오디오 포함 |

**예시:**
- 8초 영상 생성 시: $0.40 × 8 = **$3.20** (Veo 3) 또는 **$1.20** (Veo 3 Fast)

## 🔧 코드 구조

### 추가된 함수

```javascript
// server.js에 추가됨
async function generateVideoWithGeminiVeo(imageUrl, prompt, duration = 8)
```

**파라미터:**
- `imageUrl`: 업로드된 이미지 URL
- `prompt`: 영상 생성 프롬프트 (영어)
- `duration`: 영상 길이 (5-8초, 기본값: 8초)

**반환값:**
- `{ videoUrl, jobId }`: 생성된 영상 URL과 작업 ID

### API 엔드포인트

**참고**: 현재 구현된 엔드포인트는 예상값입니다. 실제 Google 공식 API 문서를 확인하여 수정이 필요할 수 있습니다.

```javascript
// 예상 엔드포인트 (확인 필요)
https://generativelanguage.googleapis.com/v1beta/models/veo-3.1:generateVideo
```

**실제 엔드포인트가 다를 경우:**
1. Google AI Studio 공식 문서 확인
2. `server.js`의 `generateVideoWithGeminiVeo` 함수 수정
3. API 요청 본문 구조 확인 및 수정

## 🚨 주의사항

### 1. API 엔드포인트 확인 필요

현재 구현된 Veo API 엔드포인트는 **예상값**입니다. 실제 Google 공식 문서를 확인하여 수정이 필요할 수 있습니다.

**확인 방법:**
1. https://ai.google.dev/docs 접속
2. Veo 3.1 API 문서 확인
3. 실제 엔드포인트 및 요청 형식 확인

### 2. 접근 권한 확인

Veo 3.1은 아직 베타 단계일 수 있으며, 접근 권한이 제한적일 수 있습니다.

**에러 메시지:**
- `403 Forbidden`: 접근 권한 없음 → 유료 계정 및 Tier 1 권한 확인
- `404 Not Found`: API 엔드포인트가 다름 → 공식 문서 확인

### 3. 비용 관리

Veo 3.1은 유료 서비스이므로 사용량을 모니터링해야 합니다.

**권장사항:**
- Google Cloud Console에서 API 사용량 확인
- 일일/월별 사용량 제한 설정
- 비용 알림 설정

## 📊 사용 통계

생성된 영상은 데이터베이스에 다음 정보가 저장됩니다:

- `ai_model`: 사용된 AI 모델 (`runway`, `runway-http`, `gemini-veo-3.1`)
- `video_url`: 생성된 영상 URL
- `job_id`: 작업 ID
- `status`: 생성 상태 (`processing`, `completed`, `failed`)

## 🔍 문제 해결

### 문제 1: "Gemini Veo 3.1 API가 아직 공개되지 않았습니다"

**원인**: API 엔드포인트가 다르거나 아직 공개되지 않음

**해결**:
1. Google AI Studio 공식 문서 확인
2. 최신 API 엔드포인트 확인
3. `server.js`의 `generateVideoWithGeminiVeo` 함수 수정

### 문제 2: "Gemini Veo 3.1 사용 권한이 없습니다"

**원인**: 유료 계정 또는 Tier 1 권한 없음

**해결**:
1. Google AI Studio에서 유료 계정 활성화
2. Veo 3.1 모델 접근 권한 확인
3. API 키가 올바른지 확인

### 문제 3: "모든 영상 생성 API 실패"

**원인**: Runway와 Gemini Veo 모두 실패

**해결**:
1. 각 API 키 확인
2. API 사용량/한도 확인
3. 네트워크 연결 확인
4. 로그에서 상세 에러 메시지 확인

## 📝 다음 단계

1. **API 엔드포인트 확인**
   - Google AI Studio 공식 문서 확인
   - 실제 Veo 3.1 API 엔드포인트 및 요청 형식 확인

2. **테스트**
   - 로컬 환경에서 테스트
   - Render 서버에서 테스트
   - 실제 영상 생성 확인

3. **비용 모니터링**
   - Google Cloud Console에서 사용량 확인
   - 비용 알림 설정

4. **사용자 선택 옵션 추가** (선택사항)
   - 사용자가 Runway 또는 Veo 중 선택할 수 있도록 UI 추가

## 🔗 참고 자료

- [Google AI Studio](https://aistudio.google.com)
- [Gemini API 문서](https://ai.google.dev/docs)
- [Veo 3.1 공식 문서](https://ai.google.dev/models/gemini/veo) (확인 필요)

