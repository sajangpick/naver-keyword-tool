# ��� 키워드검색 기능 가이드

> 최종 업데이트: 2025년 11월 2일  
> 기능명: 키워드검색 (네이버 키워드 검색 API)

---

## ��� 기능 개요

**키워드검색**은 네이버 광고 API를 활용하여 실시간 키워드 검색량과 경쟁정도를 분석하는 기능입니다.

### ��� 핵심 기능
- 월간 검색수 조회
- 클릭수 및 경쟁정도 분석
- 키워드별 트렌드 분석
- 최적 입찰가 제안

### ��� 대상 사용자
- 식당 대표/마케터
- 블로그/콘텐츠 운영자
- SEO 담당자

---

## ��� 개발 스토리

### ��� 개발 배경

사장님이 물었습니다:
> "저희 가게를 찾는 사람이 어떤 키워드로 검색할까?"

이것이 키워드검색 기능의 시작이었습니다.

### ��� 문제 분석

#### 당시 상황
```
마케팅 담당자들의 고민:

❌ "키워드를 어떻게 선택해?"
❌ "경쟁은 심한가?"
❌ "월간 검색량은?"
❌ "어디를 타겟해야 하나?"

→ 모두 추측과 경험에만 의존
```

#### 해결 방법
```
✅ 네이버 광고 API 활용
✅ 실시간 데이터 기반
✅ 정량적 분석
✅ 객관적 지표
```

### ��� 기술 구현

#### 네이버 API 연동
```javascript
// 키워드 검색 로직
async function searchKeyword(keyword) {
  const response = await naverAdvertisingAPI.getKeywordStats({
    keyword: keyword,
    monthlyData: true,
    competitionLevel: true
  });
  
  return {
    monthlySearches: response.hintKeywords[0].monthlyMobileQcCnt,
    clicks: response.hintKeywords[0].monthlyMobileClkCnt,
    competition: calculateCompetition(response),
    cpc: response.hintKeywords[0].pcCpcBidAmount
  };
}
```

#### 캐싱 전략
```javascript
// 같은 키워드는 24시간 캐시
const cache = new Map();

function getKeywordStats(keyword) {
  if (cache.has(keyword)) {
    const cached = cache.get(keyword);
    if (Date.now() - cached.timestamp < 24 * 60 * 60 * 1000) {
      return cached.data;  // 캐시 반환
    }
  }
  
  // API 호출
  const data = callNaverAPI(keyword);
  cache.set(keyword, {
    data: data,
    timestamp: Date.now()
  });
  
  return data;
}
```

### ��� 어려웠던 점과 해결

#### 1️⃣ API 할당량 문제
**문제**: 무제한 요청하면 비용이 너무 많이 들어요!

```
초기 설계:
- 매번 API 호출 → 비용 폭주 ❌

해결책:
- 24시간 캐싱 추가
- 배치 처리 (야간 시간대)
- 사용자 한도 설정
```

**결과**: 비용 80% 감소 ✅

#### 2️⃣ 데이터 정확도
**문제**: 네이버 API 데이터가 항상 최신일까?

```
발견사항:
- 네이버는 월 1회만 업데이트
- "실시간"이 아님
- 과거 데이터 활용

개선:
✅ "월별 추정값" 명시
✅ 마지막 업데이트 일자 표시
✅ 추세선으로 방향성 제시
```

#### 3️⃣ 사용성
**문제**: 너무 많은 지표가 혼란스러워요

```
초기 결과:
- 월간 검색수
- 클릭수
- CPC
- 월간 검색 변화
- 경쟁정도
- 클릭률
... 너무 많음!

개선:
✅ 핵심 3개만 표시
✅ 더 보기 버튼으로 상세 정보
✅ 추천 키워드 제시
```

### ��� 구현 결과

#### 기능 구성
```
검색창
  ↓
키워드 입력 (1-3개)
  ↓
네이버 API 호출
  ↓
결과 분석
  ├─ 월간 검색수
  ├─ 경쟁정도
  └─ 추천 입찰가
  ↓
차트로 시각화
  ↓
역사 저장
```

#### 활용 사례
```
���️ 식당 마케팅:
"돼지국밥" vs "소머리국밥" 비교
→ 어느 것이 더 인기인가?
→ 어느 것으로 마케팅할까?

��� 블로그 운영:
"서울 맛집" vs "강남 맛집" vs "관악구 맛집"
→ 어느 키워드가 더 많이 검색되나?
→ 어느 글을 먼저 쓸까?
```

### ��� 배운 점

#### 1. API는 은탄환이 아니다
```
처음: "좋은 API가 있으면 모든 게 쉬워진다"
나중: "API를 잘 사용하는 게 중요하다"

핵심: 캐싱, 한도 관리, 에러 처리 모두 필요
```

#### 2. 사용자는 간단함을 원한다
```
"모든 정보를 다 제공해야겠지?" → 오류
"가장 필요한 3개만" → 성공

데이터보다 가독성이 중요!
```

---

## ��� 기술 스택

### 프론트엔드
- `naver_search.html` - 사용자 인터페이스
- Chart.js (그래프 표시)
- 실시간 검색 결과

### 백엔드
- Express.js 서버
- axios HTTP 요청
- 네이버 광고 API
- 캐싱 미들웨어

### 외부 API
- 네이버 광고 API - 키워드 데이터
- Supabase - 검색 이력 저장

---

## ��� 사용 방법

1. **페이지 접속**: http://localhost:3003/naver_search.html
2. **키워드 입력**: 검색창에 1-3개 키워드 입력
3. **검색 실행**: "검색하기" 버튼 클릭
4. **결과 분석**: 월간 검색수, 경쟁정도 확인

---

## ��� 주요 지표 설명

- **월간 검색수**: 해당 키워드 월별 평균 검색 횟수
- **클릭수**: 광고 클릭 수 (구매의도 지표)
- **경쟁정도**: LOW / MEDIUM / HIGH
- **트렌드**: UP / DOWN / STABLE

---

## ��� 환경변수 설정

```env
NAVER_CUSTOMER_ID=xxxxx
NAVER_API_KEY=xxxxx
NAVER_SECRET_KEY=xxxxx
```

---

## ��� 활용 팁

### 식당 마케팅
- 메뉴별 키워드 검색으로 인기 메뉴 파악
- 지역+음식 조합으로 지역 특성 파악
- 계절 트렌드로 메뉴 기획

### 블로그 운영
- 검색량 많은 키워드 중심 글 작성
- 경쟁도 LOW인 키워드 우선 타겟
- 상승세 키워드로 시의적절한 정보 제공

---

> ��� 키워드검색 결과를 다른 기능과 연계하여 활용하면 더욱 효과적합니다!
