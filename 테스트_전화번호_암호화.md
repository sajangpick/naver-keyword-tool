# 전화번호 암호화 테스트 가이드

## 📍 테스트 실행 위치

**프로젝트 루트 디렉토리**에서 실행해야 합니다.

```
최종251120/  ← 여기서 실행!
├── api/
│   └── utils/
│       └── cipher_service.py
├── .env
└── 테스트_전화번호_암호화.bat
```

## 🖥️ 테스트 방법

### 방법 1: 배치 파일 사용 (Windows - 가장 쉬움)

1. 프로젝트 루트 디렉토리로 이동
2. `테스트_전화번호_암호화.bat` 파일을 **더블클릭**하거나
3. 파일 탐색기 주소창에 `cmd`를 입력하고 Enter
4. 명령 프롬프트가 열리면:
   ```cmd
   테스트_전화번호_암호화.bat
   ```

### 방법 2: 명령 프롬프트 직접 사용 (Windows)

1. **Windows 키 + R** 누르기
2. `cmd` 입력하고 Enter
3. 프로젝트 디렉토리로 이동:
   ```cmd
   cd D:\오연수사무실\개인\공부\최종251120
   ```
4. 암호화 테스트:
   ```cmd
   python api/utils/cipher_service.py encrypt "010-6664-3744"
   ```
5. 위에서 나온 결과(예: `gAAAAABk...`)를 복사
6. 복호화 테스트:
   ```cmd
   python api/utils/cipher_service.py decrypt "gAAAAABk..."
   ```
   (복사한 문자열로 대체)

### 방법 3: PowerShell 사용 (Windows)

1. 프로젝트 폴더에서 **Shift + 우클릭**
2. "PowerShell 창 여기에 열기" 선택
3. 명령어 실행:
   ```powershell
   python api/utils/cipher_service.py encrypt "010-6664-3744"
   ```

### 방법 4: VS Code 터미널 사용

1. VS Code에서 프로젝트 열기
2. **Ctrl + `** (백틱) 또는 상단 메뉴 → 터미널 → 새 터미널
3. 터미널이 열리면 자동으로 프로젝트 루트에 위치
4. 명령어 실행:
   ```bash
   python api/utils/cipher_service.py encrypt "010-6664-3744"
   ```

## ✅ 테스트 전 확인사항

### 1. Python 설치 확인

```cmd
python --version
```

출력 예시:
```
Python 3.11.0
```

Python이 없으면: https://www.python.org/downloads/ 에서 설치

### 2. cryptography 패키지 설치 확인

```cmd
pip install cryptography python-dotenv
```

### 3. .env 파일 확인

프로젝트 루트에 `.env` 파일이 있고, `SECRET_KEY`가 설정되어 있어야 합니다.

```env
SECRET_KEY=gAAAAABk... (64자리 문자열)
```

## 🎯 예상 결과

### 암호화 테스트

**입력:**
```cmd
python api/utils/cipher_service.py encrypt "010-6664-3744"
```

**출력:**
```
gAAAAABkZxYvF7Kj8LmN9OpQrStUvWxYzA1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4
```

### 복호화 테스트

**입력:**
```cmd
python api/utils/cipher_service.py decrypt "gAAAAABkZxYvF7Kj8LmN9OpQrStUvWxYzA1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2G3H4"
```

**출력:**
```
010-6664-3744
```

## ❌ 오류 해결

### 오류 1: "Python이 인식되지 않는 내부 또는 외부 명령"

**해결:**
- Python 설치 확인
- 환경 변수 PATH에 Python 추가 확인
- `python3` 명령어로 시도

### 오류 2: "SECRET_KEY가 설정되지 않았습니다"

**해결:**
1. 프로젝트 루트에 `.env` 파일 확인
2. `.env` 파일에 `SECRET_KEY=...` 추가
3. [SECRET_KEY_설정_가이드.md](./docs/SECRET_KEY_설정_가이드.md) 참고

### 오류 3: "No module named 'cryptography'"

**해결:**
```cmd
pip install cryptography python-dotenv
```

### 오류 4: "복호화 실패"

**해결:**
- 암호화된 문자열이 정확히 입력되었는지 확인 (공백 없이)
- `.env` 파일의 `SECRET_KEY`가 암호화 시와 동일한지 확인

## 📞 도움이 필요하신가요?

위 방법으로 해결되지 않으면:
1. 오류 메시지 전체를 복사
2. 어떤 방법으로 실행했는지 알려주세요
3. Python 버전: `python --version`

